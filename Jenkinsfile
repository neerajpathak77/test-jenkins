pipeline {
  agent any
  stages {
    stage ('Compile and Stage') {

        echo "qwerty"

        echo env

          steps {
              sh "npm install"
              sh "npm run build"
          }
    }
  }
}
