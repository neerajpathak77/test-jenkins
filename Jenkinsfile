pipeline {
  agent any

          echo "qwerty"

        echo env
  stages {
    stage ('Compile and Stage') {

          steps {
              sh "npm install"
              sh "npm run build"
          }
    }
  }
}
